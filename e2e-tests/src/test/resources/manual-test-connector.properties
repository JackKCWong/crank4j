# The target webserver that you want to proxy
target.uri=http://localhost:8080

# The router's registration websocket url
router.uri=wss://localhost:16488

# The port for RESTful health
connector.health.port=12448

# The socket connection pool size
connector.pool.size=1

# The service name to be registered in router
target.service.name=